{
  "name": "anki-xml",
  "version": "0.1.0",
  "description": "Import AI-authored XML flashcards into Anki via AnkiConnect",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "import": "bun run src/index.ts import",
    "test": "bun test",
    "build": "bun run scripts/build.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fast-xml-parser": "^5.10.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}